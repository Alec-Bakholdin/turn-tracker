import React from "react";
import { Card, CardContent, CardHeader } from "@mui/material";

export default function PhaseCreator(): React.ReactElement {
  return (
    <Card>
      <CardHeader title={"Game Config"} />
      <CardContent></CardContent>
    </Card>
  );
}