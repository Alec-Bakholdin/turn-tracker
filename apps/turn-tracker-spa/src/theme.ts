import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    mode: 'dark',
    background: { default: '#121928', paper: '#000000' },
    action: { active: '#132837' },
  },
});
export default theme;
